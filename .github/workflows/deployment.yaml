name: "Deployment"
on: 
  push: 
    branches: 
      - 'main'
  pull_request: 
    branches: 
      - "main"

jobs:
  hello:
    runs-on: ubuntu-latest
    steps: 
      - name: "Say hello"
        run: echo "Say hello"